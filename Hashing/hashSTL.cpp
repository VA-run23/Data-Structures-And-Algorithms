//32.2
#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define pii pair<int, int>
#define vii vector<pii>
#define rep(i,a,b) for (int i=a; i<b; i++)
#define ff first
#define ss second 
#define setBits(x) __builtin_popcount(x)

int main(){

    map<int, int> m;
    m[8] = 2;// inserts a key-value pair into the map. Here, the key is 8 and the value is 2.
    cout<<m[8]<< endl;//prints the value associated with the key 8 to the console, which is 2
    return 0;
}